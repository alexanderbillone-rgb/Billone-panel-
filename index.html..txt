<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BILLONE MODS - iOS</title>
    <style>
        :root { --gold: #ffaa00; --dark: #0a0a0a; --gray: #1a1a1a; }
        body { background: var(--dark); color: white; font-family: sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .box { width: 90%; max-width: 360px; background: var(--gray); padding: 25px; border-radius: 15px; border: 1px solid #333; text-align: center; }
        h2 { color: var(--gold); margin-bottom: 20px; text-transform: uppercase; }
        input { width: 100%; padding: 12px; margin: 10px 0; background: #000; border: 1px solid #444; border-radius: 8px; color: white; box-sizing: border-box; }
        .btn { width: 100%; padding: 15px; background: var(--gold); border: none; border-radius: 8px; font-weight: bold; cursor: pointer; margin-top: 10px; }
        .hidden { display: none; }
        .mod-item { background: #222; padding: 15px; margin: 10px 0; border-radius: 10px; display: flex; justify-content: space-between; align-items: center; border-left: 4px solid var(--gold); }
        .status { color: #0f0; font-size: 0.8rem; font-weight: bold; }
    </style>
</head>
<body>

    <div id="login-screen" class="box">
        <h2>BILLONE PANEL</h2>
        <p style="font-size: 0.8rem; color: #888;">ACCESO EXCLUSIVO iOS</p>
        <input type="text" id="user" placeholder="Usuario">
        <input type="password" id="pass" placeholder="Contraseña">
        <button class="btn" onclick="login()">ENTRAR AL PANEL</button>
    </div>

    <div id="panel-screen" class="box hidden">
        <h2>MODS ACTIVOS</h2>
        <div class="mod-item">
            <span>Calidad: <b>SUAVE</b></span>
            <span class="status">OPTIMIZADO</span>
        </div>
        <div class="mod-item">
            <span>Deslizamiento: <b>1</b></span>
            <span class="status">READY</span>
        </div>
        <div class="mod-item">
            <span>Loops: <b>1</b> (Sencillo)</span>
            <span class="status">ACTIVE</span>
        </div>
        <button class="btn" style="background:#444; color:white;" onclick="logout()">CERRAR SESIÓN</button>
    </div>

    <script>
        // Aquí puedes definir tu usuario y contraseña
        const USUARIO_VALIDO = "Billone";
        const PASS_VALIDA = "2026";

        function login() {
            const u = document.getElementById('user').value;
            const p = document.getElementById('pass').value;
            if(u === USUARIO_VALIDO && p === PASS_VALIDA) {
                document.getElementById('login-screen').classList.add('hidden');
                document.getElementById('panel-screen').classList.remove('hidden');
            } else {
                alert("Acceso denegado, Billone.");
            }
        }

        function logout() {
            location.reload();
        }
    </script>
</body>
</html>
